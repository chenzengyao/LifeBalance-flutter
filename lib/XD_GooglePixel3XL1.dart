import 'package:flutter/material.dart';

class XD_GooglePixel3XL1 extends StatelessWidget {
  XD_GooglePixel3XL1({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xffffffff),
      body: Stack(
        children: <Widget>[],
      ),
    );
  }
}
