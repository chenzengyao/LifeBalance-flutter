/*import 'package:flutter/material.dart';
import 'package:lifebalance/screens/models/course.dart';

class CourseTile extends StatelessWidget {

  final User course;
  CourseTile({ this.course});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.only(top: 8.0),   //direction
      child: Card(
        margin: EdgeInsets.fromLTRB(20.00, 6.0, 20.0, 0.0),
        child: ListTile(
          leading: CircleAvatar(
            radius: 25.0,
            backgroundColor: Colors.blue,
          ),
          title: Text(course.name),
          subtitle: Text('Course Code ${course.courseCode}'),
        )
      )
    );
  }
}*/
